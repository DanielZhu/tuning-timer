{
  "name": "tuning-timer",
  "version": "0.1.0",
  "description": "The timer which will be adjusted as time goes by.",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --exec babel-node ./specs/test.js",
    "prestart": "babel index.js --out-dir dist && babel ./specs/test.js --out-dir dist",
    "start": "node dist/index.js"
  },
  "author": "DanielZhu",
  "license": "Apache-2.0",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "nodemon": "^1.9.2"
  }
}
